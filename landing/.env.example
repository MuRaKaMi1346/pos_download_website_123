# Astro exposes PUBLIC_* variables to the client at BUILD time.
# Copy to `.env` (gitignored). In dev, the API base defaults to localhost.

# Base URL of the FastAPI backend, no trailing slash.
# Production example:
PUBLIC_API_BASE=https://api.smartbrew.app/api/v1
